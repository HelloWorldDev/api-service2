machine:
  php:
    version: 5.6.23
    version: 5.6.27
    version: 7.0.4

test:
  override:
    - php vendor/bin/phpunit --configuration phpunit.xml.dist

dependencies:
  pre:
    - composer install --no-interaction --prefer-source